services:
  mongodb:
    image: mongo:latest
    container_name: muhasebe_mongodb
    ports:
      - "27017:27017"
    volumes:
      - ./muhasebe_db:/data/db
    restart: always
    environment:
      - MONGO_INITDB_DATABASE=muhasebe_db

networks:
  default:
    name: muhasebe_network
